<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GBPL - Sign Up</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light d-flex align-items-center vh-100">
    <div class="container px-3" style="max-width: 400px;">
        <div class="card shadow border-0 rounded-3">
            <div class="card-header bg-success text-white text-center py-3">
                <h5 class="mb-0 fw-bold">Sign Up</h5>
            </div>
            <div class="card-body p-4">
                <div class="alert alert-info small text-center mb-4">
                    The first user to sign up automatically becomes the Admin.
                </div>
                <form action="/signup" method="POST">
                    <div class="mb-3">
                        <label class="form-label fw-semibold">Choose a Username</label>
                        <input type="text" name="username" class="form-control form-control-lg" required>
                    </div>
                    <div class="mb-4">
                        <label class="form-label fw-semibold">Create a Password</label>
                        <input type="password" name="password" class="form-control form-control-lg" required>
                    </div>
                    <button class="btn btn-success w-100 btn-lg fw-bold">Sign Up</button>
                </form>
                <div class="mt-4 text-center">
                    <a href="/login" class="text-decoration-none text-success">Login</a> 
                    <span class="text-muted mx-2">|</span> 
                    <a href="/" class="text-decoration-none text-secondary">Back Home</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>